export enum PATH {
  ME = '/',
  PROFILE = '/profile',
  DIALOGS = '/dialogs',
  USERS = '/users',
  LOGIN = '/login',
  ERROR_404 = '404',
}

export enum RESPONSEFIGURES {
  zeroRequest = 0,
  tenRequest = 0
}