export enum PATH {
  ME = '/',
  PROFILE = '/profile',
  DIALOGS = '/dialogs',
  USERS = '/users',
  LOGIN = '/login',
  PAGE_NOT_FOUND = '404',
}

export enum RESPONSE_NUMBER {
  COMPLETED_SUCCESSFULLY_NUMBER = 0,
  REDIRECT_TO_CAPTCHA = 10
}